// 错误：tsconfig.app.jsonc 文件内容为空或格式错误
// 解决方案：创建正确的 tsconfig.app.json 文件
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.vue"],
  "exclude": ["node_modules", "dist"]
}